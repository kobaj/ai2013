When a move to position action is set, a circle appears at that spot. The circle is created as a shadow in te getAction method.

The ship slows down as it approaches that spot. Is this to penalize the use of many smaller goals?
